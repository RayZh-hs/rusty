package rusty.ir

import rusty.semantic.support.Context

class IRConstructor {
    companion object {
        fun run(context: Context, dumpToScreen: Boolean = false): String {
            TODO()
        }
    }
}